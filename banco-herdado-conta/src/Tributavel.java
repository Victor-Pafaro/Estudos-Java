
public interface Tributavel {
	public abstract double getValorImposto(); // Poderiamos escrever apenas double getValorImposto, 
											 //pois o compilador já entende que todos os métodos das interfaces são públicos e abstratos.
}
